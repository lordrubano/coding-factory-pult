# Отчёт ВН-24 — состояние проекта при смене архитектора

Снято приборами 06.08.2026 в 16:51. Ничего не запущено и не запускается.

## Хранилище

```
HEAD              5f8c882
ветка             phase4/task-lifecycle
неотправленных    0 — отправка синхронна с origin
дерево            грязное: четыре изменённых, четыре неотслеживаемых
```

Незакоммиченное:

```
 M CLAUDE.md                             правила снятия прогона, расхода по журналу,
                                         области отметок хода
 M scripts/cf_judge.py                   разбор ответа судьи, журнал на каждый вызов
 M control_core/tests/test_cf_judge.py   случаи к ним
 M runtime/automation/mutations.json     одна спецификация перенацелена, одна снята
 M docs/CF_STEP_REGISTRY.csv             реестр приведён к факту
?? runtime/tasks/IMP-031-...-003..006.md четыре задания
```

## Процессы

```
калибровка     0
исполнитель    0
вызовы судьи   0
```

Не идёт ничего. Проверено тремя запросами к списку процессов.

## Реестр состояний

```
всего строк    120
закрыто        49
сдан           1 — IMP-031: механизм построен и закоммичен, чисел нет
в работе       нет
не начат       68
```

Две строки реестра отставали от дерева и приведены к факту этим отчётом:
`IMP-030` числился сданным при существующем коммите, `IMP-031` — не начатым при
построенном механизме. Отставание — недоведённая работа арбитра, а не дефект
прибора.

## Расход платных вызовов

```
записано в журнал   30 вызовов калибровки
годных вердиктов    17
негодных по формату 13
суммарное время     24,4 минуты
```

Журнал ведётся только с последней правки. Вызовы до неё в журнал не писались:
их число оценочно около тридцати и **измерением не является**. Итого из
разрешённых 120 израсходовано **порядка 60**, точной цифры нет и восстановить
её нечем.

## Проверки

```
регрессия      96 из 96
мутации        469: 467 убито, 2 не применились — обе перенацелены,
               после чего 54 спецификации судьи дали MUTATION_SUITE_PASS
датчик         CF_SECRETS_CLEAN
корпус         GOLDEN_OK
```

## Что сделано за смену

Личность рецензента назначена, пороги гейта предрегистрированы, оба свойства
внесены в реестр защищаемых. Гейт построен. Первый живой вызов судьи нашёл
дефект нашей изоляции — ключ `--bare` отрезал учётные данные; ключ удалён.
Затем три правки разбора подряд: разбор терял верные ответы судьи, требуя ровно
двух строк, потом чистого слова без разметки, потом вердикта строго в первой
строке. Каждая правка найдена измерением живого ответа, последняя проверена
тремя вызовами: три вердикта из трёх распознаны.

## Чего нет

**Чисел калибровки.** Судья ни разу не прошёл корпус целиком.

## Что блокирует

Разрешение потратить 108 платных вызовов. Разрешённые 120 израсходованы
примерно наполовину и без результата: они ушли на поиск причины, по которой
разбор не принимал ответы живого судьи.

```
нужно      108 вызовов
время      полтора-три часа по измеренному разбросу 14-123 секунды на вызов
деньги     порядка четырёх долларов
```

Причина перерасхода названа прямо: трижды запускался платный прогон без
проверки одним вызовом, что разбор принимает ответ живой модели. Одна проверка
за минуту сберегла бы шестьдесят вызовов.

## Что не блокировано

Механизм готов, проверки зелёные, дерево ждёт коммита. Всё, кроме калибровки,
может продолжаться без денег.
